apiVersion: v1
kind: Service
metadata:
  name: {{ include "lstolas.fullname" . }}
  labels:
    {{- include "lstolas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lstolas.selectorLabels" . | nindent 4 }}
