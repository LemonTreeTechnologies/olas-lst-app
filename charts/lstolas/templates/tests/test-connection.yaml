apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lstolas.fullname" . }}-test-connection"
  labels:
    {{- include "lstolas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lstolas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
