apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stolas.fullname" . }}-test-connection"
  labels:
    {{- include "stolas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stolas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
