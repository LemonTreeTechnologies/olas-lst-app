apiVersion: v1
kind: Service
metadata:
  name: {{ include "stolas.fullname" . }}
  labels:
    {{- include "stolas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stolas.selectorLabels" . | nindent 4 }}
